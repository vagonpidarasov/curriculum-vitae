// jscs: disable

const config = {};

config.app_dir = __dirname;
config.port = process.env.NODE_PORT || '5151';

config.api_base = '';
config.api_domian = 'http://www.mocky.io';

config.username = '%username%';
config.password = '%password%';

module.exports = config;
